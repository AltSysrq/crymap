Can we reduce the number of gravestones by forcing allocation to go through
another level of redirection? I.e. leaf directory would initially contain a
`self -> .` symlink, which gets changed to `self -> self` once directory is
fully allocated.

Command-line invocation to deliver/import mail

A way to change the user's password

A way to update key configuration

Documentation
